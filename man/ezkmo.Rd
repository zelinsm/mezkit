% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ezkmo.R
\name{ezkmo}
\alias{ezkmo}
\title{ezkmo: Display KMO values by variable}
\usage{
ezkmo(data, interpret = FALSE, gt = FALSE)
}
\arguments{
\item{data}{A data frame or tibble containing numeric variables for KMO assessment.}

\item{interpret}{Logical. If TRUE, adds interpretation labels based on standard KMO thresholds. Default is FALSE.}

\item{gt}{Logical. If TRUE, returns a formatted \code{gt} table. If FALSE, returns a tibble. Default is FALSE.}
}
\value{
A tibble or \code{gt} table summarizing KMO values per variable.
}
\description{
This function calculates and displays the Kaiser-Meyer-Olkin (KMO) Measure of Sampling Adequacy
for each variable in a data frame, using the \code{KMO()} function from the \code{psych} package.
Optionally, it adds interpretation categories and returns a formatted \code{gt} table.
}
\details{
Interpretation thresholds:
\itemize{
\item ≥ 0.90 = Marvelous
\item ≥ 0.80 = Meritorious
\item ≥ 0.70 = Middling
\item ≥ 0.60 = Mediocre
\item ≥ 0.50 = Miserable
\item <  0.50 = Unacceptable
}
}
\examples{
\dontrun{
ezkmo(mtcars)
ezkmo(mtcars, interpret = TRUE)
ezkmo(mtcars, interpret = TRUE, gt = TRUE)
}
}
