% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lrm_val_metrics.R
\name{lrm_val_metrics}
\alias{lrm_val_metrics}
\title{Bootstrap Validation Summary for \code{lrm()} Models}
\usage{
lrm_val_metrics(val_obj)
}
\arguments{
\item{val_obj}{An object returned by \code{rms::validate()} applied to an \code{lrm} model.}
}
\value{
A \code{tibble} with four rows (one per metric) and three columns:
\describe{
\item{metric}{Metric name: Dxy, C-statistic, Brier score, Nagelkerke R².}
\item{apparent}{Performance on the training data (naively optimistic).}
\item{optimism}{Average overfitting bias from bootstrap resampling.}
\item{validated}{Bias-corrected estimate: apparent − optimism.}
}
}
\description{
Performs bootstrap internal validation on a logistic regression model
fitted with \code{rms::lrm()} and returns a tidy \code{tibble} summarizing key
performance metrics. This function extracts the apparent, optimism,
and validated estimates of the following:
\itemize{
\item Somers' D rank correlation (\code{Dxy})
\item C-statistic (discrimination)
\item Brier score (calibration)
\item Nagelkerke's R-squared (explained variation)
}

The function wraps the output from \code{rms::validate()} and transforms it
into a user-friendly \code{tibble} with interpretable columns and metric names.
The C-statistic is calculated explicitly as \code{0.5 + Dxy / 2}.
}
\examples{
\dontrun{
library(rms)
data(lung, package = "survival")
lung <- na.omit(lung)

dd <- datadist(lung)
options(datadist = "dd")

fit <- lrm(status == 2 ~ age + sex + ph.ecog, data = lung, x = TRUE, y = TRUE)
val <- validate(fit, B = 200)

lrm_val_tbl(val)
}

}
