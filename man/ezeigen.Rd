% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ezeigen.R
\name{ezeigen}
\alias{ezeigen}
\title{ezeigen: Summarize eigenvalues, variance explained, and factor reliability}
\usage{
ezeigen(
  fa_object,
  data = NULL,
  digits = 2,
  gt = FALSE,
  min_loading = 0.3,
  print_items = FALSE
)
}
\arguments{
\item{fa_object}{An object returned by \code{psych::fa()} or \code{psych::principal()}.}

\item{data}{The original data frame used for factor analysis (required for alpha and omega).}

\item{digits}{Number of decimal places to round values. Default is 2.}

\item{gt}{Logical. If TRUE, returns a formatted \code{gt} table. Default is FALSE.}

\item{min_loading}{Minimum absolute loading required to assign an item to a factor. Default is 0.30.}

\item{print_items}{Logical. If TRUE, prints which items were assigned to each factor. Default is FALSE.}
}
\value{
A tibble or \code{gt} table with eigenvalues, variance explained, and reliability estimates.
}
\description{
Summarizes eigenvalues, SS loadings, variance explained, and includes Cronbach's alpha
and McDonald's omega for each factor. Items are assigned to the factor with the highest
absolute loading (optionally above a threshold). Requires original data.
}
\examples{
\dontrun{
fa_res <- psych::fa(mtcars[, 1:6], nfactors = 2)
ezeigen(fa_res, data = mtcars[, 1:6], print_items = TRUE)
}
}
