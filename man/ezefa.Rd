% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ezefa.R
\name{ezefa}
\alias{ezefa}
\title{ezefa: Item-level loadings, communalities, and diagnostics from factor analysis}
\usage{
ezefa(fa_object, data = NULL, u = TRUE, kmo = TRUE, digits = 3)
}
\arguments{
\item{fa_object}{A fitted factor analysis object from \code{psych::fa()}.}

\item{data}{The original data frame used for factor analysis (only needed if \code{kmo = TRUE}).}

\item{u}{Logical. If TRUE, includes item-level uniqueness (u²). Default is TRUE.}

\item{kmo}{Logical. If TRUE, includes item-level KMO from \code{psych::KMO()}. Default is TRUE.}

\item{digits}{Number of decimal places to round numeric values. Default is 3.}
}
\value{
A tibble with rows as items and columns for each factor loading, h², and optionally u² and KMO.
}
\description{
This function summarizes item-level factor loadings from a \code{psych::fa()} object
and appends item-level communalities (h²), and optionally, uniqueness (u²) and item-level KMO.
}
\examples{
\dontrun{
fa_res <- psych::fa(mtcars[, 1:6], nfactors = 2)
ezefa(fa_res, data = mtcars[, 1:6])
ezefa(fa_res, data = mtcars[, 1:6], u = FALSE, kmo = FALSE)
}

}
